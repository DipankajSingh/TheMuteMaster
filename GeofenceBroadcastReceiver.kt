// Assuming this is the relevant section for geofence transitions logging
if (BuildConfig.DEBUG) {
    Log.d(TAG, "Geofence transition: ENTER");
}
// existing logic for ENTER transition

if (BuildConfig.DEBUG) {
    Log.d(TAG, "Geofence transition: EXIT");
}
// existing logic for EXIT transition
